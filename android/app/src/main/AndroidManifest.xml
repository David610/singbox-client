<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"  
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Fine/coarse location: Android requires this to read the real Wi-Fi
         SSID/BSSID (network_info_plus), used by the Wi-Fi-based diversion
         rule editor (lib/screens/diversion_group_custom_edit_screen.dart)
         to populate sing-box's own `wifi_ssid`/`wifi_bssid` route-rule
         fields (see lib/app/modules/vpn_service_state.dart). KEPT: this is
         a real, end-to-end-wired sing-box routing feature, not dead code —
         removing it would be a routing-config regression, out of scope for
         a mobile-identity/dead-surface pass. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- QUERY_ALL_PACKAGES: the dead "Per-App Proxy" VPN include/exclude
         feature (lib/screens/perapp_android_screen.dart) that used to
         motivate this permission has been removed — it never actually
         wired into the native VPN tunnel (no Dart code ever populated
         SingBoxVpnService's includePackage/excludePackage options). KEPT
         anyway: full package enumeration (lib/app/utils/
         package_manager_android.dart) is shared infrastructure still used
         by two real, wired features — the diversion-rule per-app target
         picker (group.package, a genuine sing-box `package_name` route
         rule, see lib/screens/diversion_group_custom_edit_screen.dart and
         lib/app/modules/vpn_service_state.dart) and the live network
         connections screen's per-app icon/name display
         (lib/screens/net_connections_screen.dart). Removing the permission
         would break both. Still a real Play Store policy-review item
         (Play Console declaration/justification form) worth revisiting if
         either of those features is ever cut. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <!--TV-->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.hardware.faketouch" android:required="false"/>
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <uses-feature android:name="android.hardware.sensor" android:required="false"/>
    <!--TV-->
    <!-- Disable input emulation on ChromeOS -->
    <uses-feature android:name="android.hardware.type.pc" android:required="false" />
    <!--#docregion android-queries-->
    <!-- Provide required visibility configuration for API level 30 and above -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="file" />
        </intent>
    </queries>
    <!-- allowBackup="false": app-private storage (see
         lib/app/utils/local_storage.dart, lib/app/utils/path_utils.dart)
         holds live server credentials in plaintext: subscription URLs and
         tokens, VLESS UUIDs, Hysteria2 passwords, REALITY keys, the raw
         imported outbound JSON. With no explicit allowBackup or
         dataExtractionRules, Android defaults to allowBackup="true", which
         silently includes all of that in Android Auto Backup (Google
         Drive, once daily on WiFi, idle, and charging) and in `adb backup`
         for a debuggable build; neither the user nor this app's own
         explicit zip-based backup/restore feature (see
         lib/app/utils/backup_and_sync_utils.dart, which is unaffected by
         this flag) asked for that. Disabling OS-level Auto Backup entirely
         removes this exposure at zero reliability cost, since profile
         import, export, and restore already have their own dedicated
         in-app flow that does not depend on Auto Backup. -->
    <!-- Cleartext (plaintext http://) traffic is disabled by default for
         this app: subscription/profile provisioning and every other
         automatic network path require https://. A narrowly-scoped
         debug-only override for local dev servers lives in
         android/app/src/debug/AndroidManifest.xml + its
         network_security_config; it never ships in a release build. -->
    <application android:label="SingBox Client" android:name=".MyApplication"
        android:usesCleartextTraffic="false"
        android:banner="@mipmap/ic_tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:extractNativeLibs="true">
        <activity android:name=".MainActivity" 
            android:exported="true" 
            android:launchMode="singleTask" 
            android:theme="@style/LaunchTheme" 
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true" 
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <meta-data android:name="firebase_crashlytics_collection_enabled" android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" android:required="false" />  <!--  TV -->
                <action android:name="com.android_package_installer.content.SESSION_API_PACKAGE_INSTALLED" android:exported="false"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with YOUR_SCHEME://YOUR_HOST -->
                <data android:scheme="karing"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
             <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>
        <service
            android:name=".TileService"
            android:directBootAware="true"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="SingBox Client" 
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <!-- AutomationCommandReceiver (Tasker-style automation) was removed:
             it was already exported="false" (unreachable by any external
             automation app), CONNECT/RECONNECT were non-functional stubs,
             and nothing in this app's own Dart/Kotlin code ever sent these
             broadcasts internally either, so there was no real automation
             surface left to preserve. See docs/CLIENT_PRODUCTION_BASELINE.md. -->
        <provider android:name="androidx.core.content.FileProvider" android:authorities="${applicationId}.fileProvider" android:exported="false" android:grantUriPermissions="true" tools:replace="android:authorities">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" tools:replace="android:resource"/>
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data android:name="flutterEmbedding" android:value="2" />
        <!-- <meta-data android:name="io.flutter.embedding.android.EnableImpeller" android:value="true" /> -->
    </application>
</manifest>